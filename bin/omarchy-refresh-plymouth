#!/bin/bash

sudo cp ~/.local/share/nixconfig/default/plymouth/* /usr/share/plymouth/themes/nixconfig/
sudo plymouth-set-default-theme nixconfig

if command -v limine-mkinitcpio &>/dev/null; then
  sudo limine-mkinitcpio
else
  sudo mkinitcpio -P
fi
